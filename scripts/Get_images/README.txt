Libraries to install :
- pip install openpyxl
- pip install openpyxl-image-loader
- pip install Unidecode

------------------------------------------------------------

GES267-GesturesV2
GES267-Studies

You have to copy those 2 Excel files in the same directory as the file Get_images.py. The execution speed of the script depends on your hardware (normally it must take a couple of seconds).